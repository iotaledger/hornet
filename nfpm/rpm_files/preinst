getent group hornet-legacy >/dev/null || groupadd -r hornet-legacy 2>/dev/null
getent passwd hornet-legacy >/dev/null || \
    useradd -r -g hornet-legacy -G hornet-legacy -d /var/lib/hornet-legacy \
    -s /sbin/nologin -c "hornet-legacy user" hornet-legacy 2>/dev/null

# explicitly exit 0
exit 0
